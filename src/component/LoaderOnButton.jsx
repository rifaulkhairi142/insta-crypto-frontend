
const LoaderOnButton = ({className}) => {
  return (
    <div className={`loaderOnButton ${className}`}>
      <div className="justify-content-center loading-btn"></div>
    </div>
  );
};

export default LoaderOnButton;
